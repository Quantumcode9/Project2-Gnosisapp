<!DOCTYPE html>
<html lang="en">
<head>
  <!-- ... Head elements ... -->
</head>
<body>
  <h1>TV Shows</h1>
  <div class="show-list">
    <% shows.forEach(show => { %>
      <div class="show">
        <h2><%= show.name %></h2>
        <!-- More details here -->
      </div>
    <% }); %>
  </div>
</body>
</html>