
  

  


  <script>
    function removeFromWatchlist(userId, showId) {
      fetch(`/shows/watchlist/remove/${userId}/${showId}`, {
        method: 'DELETE',
      })
      .then(response => response.json())
      .then(data => {
        console.log(data);
        // Refresh the page or update the UI
      })
      .catch((error) => {
        console.error('Error:', error);
      });
    }
  </script>
  
  
  
  
  
  
  
<!--   
  
  
  






  <script>

document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.show-action').forEach((img) => {
    let timer;
    img.addEventListener('touchstart', (e) => {
      timer = setTimeout(() => {
        // Prevent default to avoid any other interaction
        e.preventDefault();
        // Get the dropdown related to this image
        const dropdownId = `dropdown${img.dataset.showId}`;
        const dropdown = document.getElementById(dropdownId);
        // Toggle the dropdown menu
        $(dropdown).find('.dropdown-menu').dropdown('toggle');
      }, 1000); // 1000ms = 1 second
    });

    // If the touch ends or moves, clear the timer
    ['touchend', 'touchmove'].forEach((eventType) => {
      img.addEventListener(eventType, () => {
        clearTimeout(timer);
      });
    });
  });
});

function rateShow(showId) {
  // Logic to handle show rating
  console.log('Rate show', showId);
  //  show a modal or another UI to capture the user rating here
}

function deleteShow(showId) {
  // Logic to handle show deletion
  console.log('Delete show', showId);
    //show a modal or another UI to confirm the deletion here
}

  </script> --> 